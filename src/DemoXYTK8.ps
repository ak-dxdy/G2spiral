%!
% gv           -arg="-sFname=DemoInp.txt"  G2spiral.ps &
% gv -infoAll  -arg="-sFname=DemoInp.txt"  G2spiral.ps &

/WorkData <<
   /OutputN false
%  /OutputFile (/home/ak/tmp/VogtSpiral.txt) 
   /UserG2Data  [/XYTK8 0   0  -105 -.4      1. 1.  -75  0.3]
   /UserPhiData 5.
   /MaxLength 40   % 30 to test all rejected

%    To use radius instead of curvature, you can use the following PostScript syntax:
%     -1. 3. div   % "div" operator replaces two preceeding operands by their quotient.
>> def

